# RK3568 上位机接口文档

本文面向 `D:\projects_D\code\嵌赛\2Dpan-tilt` 当前固件版本，目标是让 RK3568 侧直接按本文实现 USB CDC 上位机程序。

## 1. 总览

- 物理链路：`USB FS CDC`
- 角色分工：
  - `RK3568`：USB Host，上位机协议主发起方
  - `STM32F407VET6`：USB Device，负责双舵机控制和状态回传
- 调试串口：`USART1 / PA9 PA10 / 115200 8N1`
  - 只输出日志
  - 不承载业务协议

## 2. 上位机职责

- 周期发送 `TRACK` 帧，把目标误差交给 STM32
- 需要调参时发送 `PARAM_GET` / `PARAM_SET`
- 需要回中、强制上报状态、切换待机时发送 `CONTROL_CMD`
- 接收并解析 STM32 返回的 `ACK` 和 `STATUS`

建议：

- STM32 控制频率约 `100 Hz`
- RK3568 目标检测频率可以低于此值，例如 `7~15 Hz`
- 只要每次检测结果更新后及时发一帧 `TRACK` 即可
- 若当前帧目标无效，继续发 `TRACK`，但清除 `TARGET_VALID`

## 3. 串口帧格式

所有业务消息都使用同一二进制帧格式：

```text
AA 55 | msg_type | flags | payload_len | frame_id_le(2) | capture_ts_ms_le(4) | payload | xor | 0D
```

字段定义：

| 字段 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| `SOF0` | 1 | 固定值 | `0xAA` |
| `SOF1` | 1 | 固定值 | `0x55` |
| `msg_type` | 1 | `uint8` | 消息类型 |
| `flags` | 1 | `uint8` | 标志位 |
| `payload_len` | 1 | `uint8` | 负载长度 |
| `frame_id` | 2 | `uint16_le` | 帧号 |
| `capture_ts_ms` | 4 | `uint32_le` | 图像采集时刻，单位 `ms` |
| `payload` | N | bytes | 具体业务负载 |
| `checksum` | 1 | `uint8` | 从 `msg_type` 到 `payload` 的逐字节异或 |
| `tail` | 1 | 固定值 | `0x0D` |

约束：

- 多字节字段全部使用小端
- 最大 `payload_len = 160`
- 最大整帧长度 `173` 字节
- `checksum` 不包含帧头 `AA 55` 和尾字节 `0D`

## 4. frame_id 定义

`frame_id` 是上位机自己维护的请求序号，用来做三件事：

- 让 RK3568 把 `ACK` 与自己发出的请求一一对应
- 让 `STATUS.last_frame_id` 标记“STM32 最近一次处理的 TRACK 来自哪一帧”
- 辅助日志排查乱序、丢帧、超时

建议做法：

- 每发一帧请求，`frame_id` 自增 1
- 用 `uint16_t` 环绕即可，不需要停机重置
- RK3568 收到 `ACK` 时，按同一 `frame_id` 匹配原请求

注意：

- `ACK.frame_id` 等于被应答请求的 `frame_id`
- `STATUS.frame_id` 是 STM32 自己的状态帧发送序号，不等于最近一次请求号
- `STATUS.last_frame_id` 才表示最近处理过的 `TRACK.frame_id`

## 5. flags 定义

请求帧 `flags`：

| 位 | 名称 | 值 | 说明 |
|---|---|---:|---|
| bit0 | `TARGET_VALID` | `0x01` | 当前目标有效 |
| bit1 | `ACK_REQUEST` | `0x02` | 请求 STM32 回 ACK |
| bit2 | `STATUS_REQUEST` | `0x04` | 请求 STM32 额外回一帧 STATUS |

一般建议：

- `TRACK`：通常带 `ACK_REQUEST | STATUS_REQUEST`，即 `0x06` 或 `0x07`
- `PARAM_SET` / `PARAM_GET` / `CONTROL_CMD`：`flags` 可置 `0x00`
  - 这些命令当前固件本身就会回 ACK

## 6. 消息类型

| 名称 | 值 | 方向 | 说明 |
|---|---:|---|---|
| `TRACK` | `0x01` | RK3568 -> STM32 | 发送目标误差 |
| `PARAM_SET` | `0x02` | RK3568 -> STM32 | 在线写参数 |
| `PARAM_GET` | `0x03` | RK3568 -> STM32 | 在线读参数 |
| `CONTROL_CMD` | `0x04` | RK3568 -> STM32 | 控制命令 |
| `STATUS` | `0x81` | STM32 -> RK3568 | 状态上报 |
| `ACK` | `0x82` | STM32 -> RK3568 | 应答帧 |

## 7. TRACK 接口

### 7.1 payload

固定 4 字节：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 2 | `int16_le` | `err_x` |
| 2 | 2 | `int16_le` | `err_y` |

### 7.2 误差定义

建议 RK3568 直接发送“归一化后的有符号误差”，不要发原始像素误差。

建议定义：

- `err_x > 0`：目标在图像中心右侧，需要云台水平轴向“正方向”修正
- `err_x < 0`：目标在图像中心左侧
- `err_y > 0`：目标在图像中心下侧，需要云台竖直轴向“正方向”修正
- `err_y < 0`：目标在图像中心上侧

建议量纲：

- 先把像素误差归一化到固定小范围
- 推荐初始范围：`[-500, 500]` 或 `[-1000, 1000]`

这样做的好处：

- STM32 不需要知道分辨率
- 不需要在 STM32 再做像素到控制量的换算
- 只需要通过 `kp_num / kp_den / max_step_us / invert / Kalman` 调手感

当前实现说明：

- RK3568 发送的是原始观测误差
- STM32 在有效 `TRACK` 到达时，先做一维 Kalman 观测滤波，再进入控制器
- 如果某轴 `KALMAN_ENABLE=0`，该轴会直接旁路原始误差

### 7.3 有效目标与无效目标

- 检测到有效目标：
  - `flags` 置 `TARGET_VALID`
  - `payload` 放入本帧 `err_x / err_y`
- 检测不到目标：
  - 仍发送 `TRACK`
  - 但清除 `TARGET_VALID`
  - `payload` 可填 `0, 0`

当前 STM32 行为：

- `TARGET_VALID = 1`：进入或保持 `TRACKING`
- `TARGET_VALID = 0`：进入 `HOLD_LAST`

### 7.4 capture_ts_ms

当前建议填“图像采集时刻”的毫秒时间戳。

用途：

- 让 STM32 回传时带出同一时间戳，便于端到端观测延迟
- 便于未来扩展预测或滤波

当前版本说明：

- STM32 会保存并在 `STATUS.last_capture_ts_ms` 中回传
- STM32 当前不使用它做超时判断
- 真正的目标超时依据是 STM32 本地收到 `TRACK` 的时间
- 当前 Kalman 也不按 `capture_ts_ms` 做变步长更新，而是在每次有效 `TRACK` 到达时更新一次

## 8. PARAM_SET 接口

### 8.1 payload

按 6 字节为一组：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `param_id` |
| 1 | 1 | `uint8` | `axis_id` |
| 2 | 4 | `int32_le` | `value` |

支持一次写多组，`payload_len` 必须是 `6` 的整数倍。

### 8.2 axis_id

| 名称 | 值 |
|---|---:|
| `PAN` | `0` |
| `TILT` | `1` |
| `ALL` | `0xFF` |

### 8.3 param_id

轴参数：

| param_id | 名称 | 说明 |
|---|---|---|
| `0x01` | `DEADBAND` | 死区 |
| `0x02` | `MAX_STEP_US` | 单周期最大脉宽变化量 |
| `0x03` | `KP_NUM` | 比例分子 |
| `0x04` | `KP_DEN` | 比例分母 |
| `0x05` | `CENTER_US` | 中位脉宽 |
| `0x06` | `HOME_US` | 回中/待机脉宽 |
| `0x07` | `MIN_US` | 脉宽下限 |
| `0x08` | `MAX_US` | 脉宽上限 |
| `0x09` | `INVERT` | 方向反转，`0/1` |
| `0x0A` | `KALMAN_ENABLE` | 当前轴是否启用 Kalman，`0/1` |

全局参数：

| param_id | 名称 | axis_id |
|---|---|---|
| `0x20` | `STATUS_PERIOD_MS` | `0xFF` |
| `0x21` | `TARGET_TIMEOUT_MS` | `0xFF` |
| `0x22` | `BOOT_CENTER_MS` | `0xFF` |
| `0x23` | `KALMAN_Q_MILLI` | `0xFF` |
| `0x24` | `KALMAN_R_MILLI` | `0xFF` |

### 8.4 返回

`PARAM_SET` 成功时，STM32 返回：

- `ACK.msg_type = 0x82`
- `ACK.payload[0] = 0x02`
- `ACK.payload[1] = 0x00`
- `ACK.detail` 中返回“实际生效值”

如果传入参数超限，STM32 会先按规则整理，再把整理后的值返回。

调 Kalman 时建议：

- `KALMAN_Q_MILLI` 越大，跟随越快，但平滑性越弱
- `KALMAN_R_MILLI` 越大，更稳更平滑，但响应更慢
- 对全局参数，返回记录中的 `axis_id` 统一是 `0xFF`

## 9. PARAM_GET 接口

### 9.1 请求格式

- 空 `payload`：读取全部参数
- 非空 `payload`：按 2 字节一组请求

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `param_id` |
| 1 | 1 | `uint8` | `axis_id` |

### 9.2 返回格式

通过 `ACK(OK)` 返回参数内容。

`ACK.detail` 按 6 字节一组组织：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `param_id` |
| 1 | 1 | `uint8` | `axis_id` |
| 2 | 4 | `int32_le` | `value` |

当前空 payload 的全量返回包含：

- `PAN` 轴 10 项参数
- `TILT` 轴 10 项参数
- 5 项全局参数

## 10. CONTROL_CMD 接口

payload 结构：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `cmd_id` |
| 1 | 0/1 | `uint8` | `arg0`，部分命令使用 |

命令定义：

| 命令 | 值 | 说明 |
|---|---:|---|
| `GO_HOME` | `0x01` | 清除目标，回 home，并进入 `STANDBY` |
| `SET_STANDBY` | `0x02` | 清除目标，回 home，并进入 `STANDBY` |
| `SET_HOLD_LAST` | `0x03` | 进入 `HOLD_LAST` |
| `CLEAR_TARGET` | `0x04` | 清除目标，非开机归中阶段转 `HOLD_LAST` |
| `FORCE_STATUS` | `0x05` | 立即请求一帧 STATUS |
| `SET_STATE` | `0x06` | 用 `arg0` 指定状态 |

`SET_STATE.arg0` 支持：

| 值 | 状态 |
|---:|---|
| `1` | `STANDBY` |
| `2` | `TRACKING` |
| `3` | `HOLD_LAST` |

限制：

- 不允许强制设成 `BOOT_CENTERING`
- 当没有有效目标时，不允许强制进入 `TRACKING`

## 11. STATUS 接口

payload 固定 32 字节：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `state` |
| 1 | 1 | `uint8` | `status_flags` |
| 2 | 2 | `uint16_le` | `last_frame_id` |
| 4 | 2 | `int16_le` | `last_err_x` |
| 6 | 2 | `int16_le` | `last_err_y` |
| 8 | 2 | `uint16_le` | `pan_us` |
| 10 | 2 | `uint16_le` | `tilt_us` |
| 12 | 2 | `uint16_le` | `pan_compare` |
| 14 | 2 | `uint16_le` | `tilt_compare` |
| 16 | 4 | `uint32_le` | `last_capture_ts_ms` |
| 20 | 4 | `uint32_le` | `last_track_rx_ms` |
| 24 | 4 | `uint32_le` | `status_period_ms` |
| 28 | 4 | `uint32_le` | `target_timeout_ms` |

状态值：

| 值 | 名称 |
|---:|---|
| `0` | `BOOT_CENTERING` |
| `1` | `STANDBY` |
| `2` | `TRACKING` |
| `3` | `HOLD_LAST` |

`status_flags`：

| 位 | 名称 | 值 | 说明 |
|---|---|---:|---|
| bit0 | `TARGET_VALID` | `0x01` | 当前目标有效 |
| bit1 | `TARGET_SEEN` | `0x02` | 曾经接收过目标 |
| bit2 | `ACK_PENDING` | `0x04` | ACK 队列非空 |
| bit3 | `STATUS_PENDING` | `0x08` | 待发状态帧存在 |
| bit4 | `RX_OVERFLOW` | `0x10` | 接收 FIFO 溢出 |
| bit5 | `ACK_OVERFLOW` | `0x20` | ACK 队列溢出 |

说明：

- `last_err_x / last_err_y` 是 STM32 当前用于控制的最近误差
- 若对应轴 `KALMAN_ENABLE=1`，这里是滤波后的误差
- 若对应轴 `KALMAN_ENABLE=0`，这里是原始误差

## 12. ACK 接口

payload 结构：

| 偏移 | 长度 | 类型 | 说明 |
|---|---:|---|---|
| 0 | 1 | `uint8` | `acked_msg_type` |
| 1 | 1 | `uint8` | `ack_code` |
| 2 | N | bytes | `detail` |

`ack_code`：

| 值 | 名称 | 说明 |
|---:|---|---|
| `0` | `OK` | 成功 |
| `1` | `BAD_LENGTH` | 长度错误 |
| `2` | `BAD_PARAM` | 参数错误 |
| `3` | `BAD_CMD` | 命令非法 |
| `4` | `BAD_STATE` | 当前状态不允许 |
| `5` | `BAD_MSG` | 消息类型非法 |
| `6` | `BUFFER_TOO_SMALL` | 返回缓冲区不足 |

说明：

- `ACK.frame_id` 与被应答请求完全一致
- 当前 ACK 队列长度为 `8`
- ACK 发送优先级高于 STATUS

## 13. 当前默认参数

轴参数默认值：

| 参数 | PAN | TILT |
|---|---:|---:|
| `deadband` | 4 | 4 |
| `max_step_us` | 20 | 20 |
| `kp_num` | 1 | 1 |
| `kp_den` | 8 | 8 |
| `center_us` | 1500 | 1500 |
| `home_us` | 1500 | 501 |
| `min_us` | 500 | 500 |
| `max_us` | 2500 | 2500 |
| `invert` | 0 | 0 |
| `kalman_enable` | 1 | 1 |

全局参数默认值：

- `status_period_ms = 100`
- `target_timeout_ms = 250`
- `boot_center_ms = 300`
- `kalman_q_milli = 16000`
- `kalman_r_milli = 64000`

机械定义说明：

- 当前默认 `PAN.home_us = 1500 us`
- 当前默认 `TILT.home_us = 501 us`
- 该 `TILT.home_us` 以当前安装方式定义为“回中后朝小车正前方”
- 已于 `2026-05-14` 完成实机联调确认
- 该值已接近当前 `TILT.min_us = 500 us`，若后续更换支架、舵盘安装角度或摄像头姿态，应重新标定 `HOME_US`，必要时连同 `MIN_US / MAX_US` 一起调整

## 14. 推荐上位机时序

1. RK3568 打开 USB CDC
2. 等待 STM32 上电归中结束，建议至少等待 `300 ms`
3. 发送一次 `CONTROL_CMD(FORCE_STATUS)`
4. 收到 `STATUS` 后确认当前状态与默认参数
5. 用一次 `PARAM_GET(all)` 记录当前 `KALMAN_ENABLE / KALMAN_Q_MILLI / KALMAN_R_MILLI`
6. 开始按视觉结果周期发送 `TRACK`
7. 若目标丢失，继续发 `TRACK` 但清除 `TARGET_VALID`
8. 调参时使用 `PARAM_SET`，并解析返回的 `ACK.detail`
9. 需要验证写入结果时，再对目标参数做一次精确 `PARAM_GET`

## 15. 推荐异常处理

- 如果 `STATUS` 长时间没有更新：
  - 先检查 USB CDC 是否断开重枚举
  - 再检查是否只发了请求但没做接收线程
- 如果 `ACK.code != OK`：
  - `BAD_LENGTH`：先核对 payload 长度
  - `BAD_PARAM`：先核对 `param_id / axis_id / value`
  - `BAD_STATE`：通常是无有效目标时强制切到 `TRACKING`
- 如果 `status_flags` 出现 `RX_OVERFLOW`：
  - 降低发送突发
  - 避免一次堆积大量参数写命令
- 如果机械方向与误差正负号相反：
  - 优先通过 `INVERT` 参数修正
  - 不建议改协议中 `err_x / err_y` 的正负定义
- 如果你感觉跟踪太抖：
  - 优先增大 `KALMAN_R_MILLI`
- 如果你感觉跟踪太慢：
  - 优先增大 `KALMAN_Q_MILLI`，或适当减小 `KALMAN_R_MILLI`

## 16. 十六进制示例

### 16.1 请求一帧状态

```text
AA 55 04 00 01 01 00 00 00 00 00 05 01 0D
```

说明：

- `msg_type = 0x04` -> `CONTROL_CMD`
- `payload = 05` -> `FORCE_STATUS`

### 16.2 发送有效目标

示例含义：

- `frame_id = 3`
- `capture_ts_ms = 0x12345678`
- `err_x = 10`
- `err_y = -6`
- `flags = TARGET_VALID | ACK_REQUEST | STATUS_REQUEST = 0x07`

```text
AA 55 01 07 04 03 00 78 56 34 12 0A 00 FA FF 16 0D
```

### 16.3 发送无效目标

```text
AA 55 01 06 04 04 00 79 56 34 12 00 00 00 00 3A 0D
```

说明：

- `flags = ACK_REQUEST | STATUS_REQUEST = 0x06`
- 未置 `TARGET_VALID`

## 17. 最小实现建议

RK3568 上位机至少需要三个模块：

- 发送模块：组帧、计算异或、维护 `frame_id`
- 接收模块：按字节流拆帧，不要假设一次 `read()` 就是一整帧
- 状态模块：缓存最近一次 `STATUS`，并按 `ACK.frame_id` 匹配请求

如果你后面要我继续，我可以直接再给你补一份：

- `C/C++` 版 RK3568 组帧与拆帧示例
- `Python` 版快速联调脚本
- `C++ class` 风格的上位机协议封装骨架
